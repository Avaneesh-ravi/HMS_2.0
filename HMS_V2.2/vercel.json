{
  "builds": [
    {
      "src": "router.php",
      "use": "vercel-php@0.7.3",
      "config": {
        "includeFiles": ["**/*.php", "**/*.css", "**/*.js"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "router.php" }
  ]
}